// Backwards-compatible import path for extensions compiled against the
// temporary adapter boundary introduced during the provider-router migration.
export { GoogleAdapter as GoogleLegacyAdapter } from "./googleAdapter.js";
