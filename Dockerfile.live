FROM node:22-bookworm-slim

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .
RUN npm run build
RUN mkdir -p /app/data /app/sandbox && chown -R node:node /app

ENV NODE_ENV=production
ENV PORT=5000
ENV GRPC_PORT=50051

EXPOSE 5000

USER node

CMD ["node", "dist/index.cjs"]
